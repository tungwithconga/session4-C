#include <stdio.h>
 int main(){
 	int a,b,c;
 	printf ("canh a: ");
 	scanf ("%d",&a);
 	printf ("canh b: ");
 	scanf ("%d",&b);
 	printf ("canh c: ");
 	scanf ("%d",&c);
    if (a==c || a==b || b==c){
    	 printf ("tam giac can");
	}else if (a=b=c){
		printf("tam giac deu");
	}else (a+b>c || b+c>a || a+c>b);
	printf("tam giac thuong") 
	  
	  
 }
  
